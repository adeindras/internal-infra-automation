# This manifest was generated by flux. DO NOT EDIT.
---
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: GitRepository
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 60m0s
  ref:
    branch: master
  secretRef:
    name: flux-system
  url: ssh://git@bitbucket.org/accelbyte/iac.git
  ignore: |-
    # exclude all
    /*
    # include client manifests dir
    !/manifests/clusters/${CUSTOMER_NAME}/${PROJECT}/${AWS_REGION}/${ENVIRONMENT_NAME}/
    # include manifests/platform dir
    !/manifests/platform/
---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./manifests/clusters/${CUSTOMER_NAME}/${PROJECT}/${AWS_REGION}/${ENVIRONMENT_NAME}/cluster-system
  prune: false
  sourceRef:
    kind: GitRepository
    name: flux-system
